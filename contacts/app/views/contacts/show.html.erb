<p id="notice"><%= notice %></p>

<div class="bg-light">
  <!--%= link_to avatar_path do %-->
   <%=link_to image_tag(@contact.image, style: "width: 400px;, height: 400 display: block"), @contact.image %>
  <!--% end %-->

  <p class="fs-2">
    <strong>First name:</strong>
    <%= @contact.first_name %>
  </p>

  <p class="fs-2">
    <strong>Last name:</strong>
    <%= @contact.last_name %> 
  </p>

  <p class="fs-4">
    <strong>Media:</strong>
   <!--- cada bloque hace los mismo --> 

    <!--%= @links.map{|link| link.media} %-->
   <!---------->
    <!--% @links.each do |link| %-->
      <!--p--><!--%= link.media %--><!--/p-->
    <!--% end %-->
   <!------------>   
    <%= render partial: "links_show", collection: @links %>

    
  </p>

  <p class="fs-6">
    contact listed by:
    <%= @contact.user.name %>
  </p>

</div>

<%= link_to 'Edit', edit_contact_path(@contact), class:"btn btn-success" %> 
<%= link_to 'Back', contacts_path, class:"btn btn-danger" %>
